cargo run -- arg

// Error handling
fn my_func() -> Result<T, Box<dyn std::error::Error>> {
    let x = might_fail()?;
    Ok(x)
}

// Optional values
if let Some(value) = maybe_value {
    // use value
}

// Iterating
for item in collection {
    // use item
}

// Creating collections
let mut vec = Vec::new();
vec.push(item);